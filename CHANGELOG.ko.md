# 변경 이력

Wimple의 주요 변경 사항을 이 문서에 기록합니다.

[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 참고합니다. 단,
이 프로젝트는 엄밀한 SemVer 대신 단일 `versionCode` 체계를 사용합니다. 날짜는
ISO 8601 (`YYYY-MM-DD`) 표기를 따릅니다.

영문판: [CHANGELOG.md](CHANGELOG.md)

## [1.0.53] — 2026-05-11 (현재)

프로젝트 역사상 가장 큰 릴리스. 단말 내 AI를 활용한 은행 알림 자동 분류,
홈 화면 위젯, Material 3 전면 적용, 그리고 다수의 보안 강화가 포함됩니다.

### 추가됨

#### 은행 알림 AI 자동 분류 (베타)
- 단말 내 분류기(ML Kit Prompt API를 통한 Gemini Nano) — 한국어 은행 알림에서
  가맹점·금액·분류(지출/수입/이체)를 추출해 거래 초안으로 만듭니다.
- 검수 큐 화면 — 상태 배지(Ready / Ambiguous / Unparsed / Error), 출처 태그
  (학습된 매핑 vs AI 추천), `[확정]` 한 번 등록, `[수동 입력]`, 무시, 재시도,
  일괄 확정 지원.
- 분류 파이프라인 — 추출 → `MerchantMappingDBHandler` 매핑 조회 → 최근
  거래 기반 AI 유사도 → 계정 페어 추천. 사용자가 확정한 예시 풀
  (`ExtractionExampleDBHandler`)을 few-shot prompt에 활용.
- 수동 입력 화면 인계 — 제목/금액/계정 자동 채움, AI가 채운 필드는 자주 쓰는
  항목 캐스케이드로부터 보호, 사용자가 최종 선택한 계정 페어를 매핑 저장소에
  학습.
- 정규식 금액 fallback (`extractAmountFromText`) — AI가 금액을 추출하지 못한
  경우 원본 알림 텍스트에서 정규식으로 추출해 form에 미리 채워줍니다. 빈
  필드 대신 의미 있는 시작점 제공.
- 베타 표기를 모든 진입점에 적용 — 설정 토글, 첫 사용 추천 다이얼로그, 활성화
  후 안내 다이얼로그, 활성화 토스트. 분류 실패 시 수동 입력으로 처리됨을
  bullet로 안내.
- 단발성 추천 다이얼로그가 기존 생체 인증 onboarding을 대체 — 단말이 on-device
  AI를 지원할 때 한 번만 노출.
- 개발자 전용 AI 분류 로그 뷰어 — "오픈소스 라이선스" 항목을 10번 탭하면
  활성화. 단계별 prompt / response / 후보 / 매핑 조회 결과를 단말에서 추적
  가능.

#### 툴바 알림 동선
- 후잉 게시판 새 알림 toolbar 아이콘 — 앱 진입 시 처음 등장하는 순간 1회
  흔들림 애니메이션.
- 은행 알림 검수 대기 toolbar 아이콘 — 동일하게 첫 등장 시 흔들림.
- 각 아이콘 아래에 짧은 안내 버블 ("은행 알림 N건" / "후잉 게시판 새 알림")이
  2.5초간 노출되고 자동 dismiss.

#### 월별 요약 홈 화면 위젯
- 현재 월의 일자별 수익·지출·순액을 그래프와 함께 표시하는 위젯 신설. 이전/
  다음 달 전환 지원.

#### 기타
- 한국어 사용 가이드와 개발자 위키를 `docs/`에 추가.
- 은행 알림 파싱 / 계산기 유틸리티에 대한 단위 테스트.

### 변경됨

- Material 3 디자인 시스템 전면 정비 — 컬러 토큰, 타이포그래피, 리스트 항목
  구분, 거래 입력 레이아웃 밀도, 키패드 `=` 키 가시성 복원.
- AI 분류기 prompt를 작은 모델용으로 재작성 — 스키마 우선 배치, 한국어 kind
  라벨(지출/수입/이체) + 영어 fallback 파싱, 연속값 대신 이산형 confidence
  (`high` / `medium` / `low`), few-shot 예시에 Title + Body 모두 포함하여
  라이브 알림 형식과 일치, `MAX_CANDIDATES` 30 → 10 축소로 lost-in-the-middle
  완화.
- 알림 캡처와 후잉 전송이 분리됨 — one-tap AI 추천이 독립적으로 동작.
- 검수 세션의 계정 리스트 선택이 adapter reload에서도 유지됨 (기존: 세션 내
  여러 차례 발생하는 `GET_ALL_ACCOUNT_RECEIVED`가 시각적 선택을 지워버림).
- 분류 진행 중 사용자가 무시한 행은 큐에서 즉시 제외되어 모델 시간 낭비 방지.

### 수정됨

- 위젯의 수익 / 지출 합계가 whooing.com 표시와 일치하도록 보정.
- 일부 레이아웃에서 잘리던 계산기 `=` 키 가시성 복원.
- 거래 입력 화면과 리스트 항목 사이 geometry 통일.

### 보안

- 앱 데이터 자동 백업 비활성화 — 인증 토큰 등 민감 정보가 Android 기본 백업
  경로로 새지 않도록 차단.
- trust-all SSL 구성 제거 — HTTPS는 표준 인증서 검증을 따름.
- 후잉 앱 시크릿을 추적 소스 밖으로 이동.
- Wimple 인증 저장소를 별도 모듈로 분리.
- 후잉 API 요청을 JSON / form-encoded 페이로드 기준으로 표준화.

## [1.0.52] — 2026-05-01

### 변경됨

- 1.0.51에서 도입한 R8 minify와 리소스 shrinking을 Play Store 크래시 보고로
  인해 복원. 빌드는 unminified 상태로 출시됨.

## [1.0.51] — 2026-04-30

대규모 UI / 테마 / 폴더블 릴리스. R8 minify + 리소스 shrinking을 처음 시도했으나
1.0.52에서 복원.

### 추가됨

- sw600dp+ 화면(폴더블, 대화면 태블릿)에서 드로어 two-pane 페어링.
- Two-pane 설정 레이아웃과 Wimple 문의 링크.
- FAB 위치를 폴드 상태(접힘/펼침)별로 별도 저장.
- FAB 기본 회전을 6개 화면으로 확장.
- 미지원 알림 수집 화면 — 파서가 처리하지 못한 은행 알림을 수동 검수.
- 어느 앱의 알림을 읽을지 선택하는 앱 선택기.
- 은행 알림 업로드 파이프라인(알림 단위 후잉 전송).
- 오픈소스 라이선스 안내 화면.
- 첫 실행 시 약관 동의 안내.
- 생체 인증 정보 다이얼로그.
- 개발 워크플로우용 Claude Code 로컬 설정(`.claude/`).

### 변경됨

- `AppTheme`을 `Theme.Material3.DayNight.NoActionBar`로 마이그레이션. 라이트
  팔레트만 유지(다크 변종을 잠시 시도 후 복원).
- Material 3 시맨틱 색상 토큰 도입.
- `appcompat` + `material`을 Material 3 테마를 제공하는 버전으로 업그레이드.
- Kotlin / 빌드 스크립트 현대화.
- Wimple 수명 주기 리팩토링 — 리스너, fragment 생성, FAB 컨트롤러, 생체 인증
  구현, 커맨드 ID를 `WimpleActivity`에서 분리.
- 은행 알림 업로더를 후잉 클라이언트 포맷에 맞게 갱신.

### 수정됨

- 폴더블에서 two-pane이 표시되지 않던 초기 회귀.
- 로그아웃 시 알림 전송 옵션이 유지되지 않던 문제.
- 일부 진입 경로의 Wimple 시작 / 재시작 이슈.
- 알림 업로드 및 저장 경로.
- 분리된(detached) `TwoPaneFragment` 상태에 대한 다중 가드 수정.

## [1.0.49] — 2025-10-10

### 변경됨
- 사용 중단(deprecated) API를 현재 AndroidX 표준으로 갱신.

## [1.0.48] — 2025-10-10

### 변경됨
- `targetSdkVersion`을 최신 레벨로 상향.
- Gradle 빌드 스크립트 갱신.
- 최신 Kotlin 컴파일러에서 발생하던 빌드 오류 수정.

## [1.0.46] — 2023-06-29

### 추가됨
- 프로젝트 / 빌드 설정 갱신.

### 변경됨
- 사용 중단 preference API 교체.

### 수정됨
- 최신 항목 정렬 이슈.
- 거래 목록이 캐시된 entry를 사용하도록 보정.
- 항목명 검색 시 한글 초성 비교.

## [1.0.45] — 2021-11-22

### 수정됨
- 로그인 차단 이슈 수정 및 오류 처리 개선.

## [1.0.44] — 2021-11-16

### 수정됨
- 일부 버튼 클릭 경로의 크래시.
- 멀티윈도우 이슈 — fragment를 매번 재생성해 상태 일관성 유지.

## [1.0.43] — 2020-11-04

### 변경됨
- 리스트 색상 토큰의 alpha 상향.
- 거래 입력 페이지의 alpha 조정.

### 수정됨
- DB 핸들러 null 미체크로 인한 크래시.

## [1.0.41] — 2020-09-24

### 수정됨
- 계정 expandable list 렌더링 이슈.
- 대문자 최신 항목 찾기.
- 영문 최신 항목 찾기.

### 변경됨
- `targetSdkVersion`을 28로 상향.

## [1.0.40] — 2020-04-21

### 추가됨
- 앱 실행 시 생체 인증.
- 생체 인증 토글 설정 항목.
- 최신 항목 리스트에 한글 초성 검색.

### 변경됨
- 시작 시 불필요한 API 호출 제거.

### 수정됨
- Fragment 교체 중 발생하던 크래시.
- Fragment 추가 시 `IllegalStateException`.

## [1.0.39] — 2019-03-24

### 추가됨
- FAB가 fragment에 따라 적절한 아이콘을 표시.
- FAB 커스터마이즈 설정.
- FAB 아이콘 리소스 세트.

### 수정됨
- 초기 로드 시 메모 입력 패드 표시.
- 날짜 파싱 이슈.
- 거래 목록 업데이트 표시.

### 변경됨
- AndroidX 패키지 버전 갱신.

## [1.0.38] — 2019-03-15

### 변경됨
- `ItemManager`와 `EntryManager`를 정돈된 구현으로 교체.
- `RestAPIInvoker` 교체.
- 디바이스 설정에 따른 로케일 적용.

### 수정됨
- 섹션 선택 및 entry 제거.
- onResume 시 entry 설정.

## [1.0.37] — 2019-03-04

프로젝트 전반 현대화 릴리스.

### 변경됨

- Android Support Library에서 AndroidX / Jetpack으로 마이그레이션.
- 모든 Activity / Fragment를 Kotlin 친화적 패턴으로 전환 — splash, main,
  settings, payment notice, transaction list, income / expense summary,
  post-news, datepicker, insert.
- 설정, payment notice, 거래 목록, 입력 fragment 교체.
- IME 기반 금액 입력 지원.
- 빌드 스크립트 전면 정비 — CircleCI 통합, lint 옵션.
- 레거시 SMS 읽기 / 전송 기능 제거.

### 수정됨

- 시작 시 크래시.
- 금액이 비어있을 때 새로고침 이슈와 크래시.
- 설정 화면 표시 크래시.
- 날짜 필터 이슈.
- 다수의 Kotlin lint 경고 정리.

## [1.0.35] — 2018-02-13

### 수정됨
- 미니 계산기의 마이너스 값.
- 계산 후 0 입력.

### 변경됨
- 코드 포맷팅 정리.

## [1.0.34] — 2018-02-11

### 추가됨
- 후잉 외부입력용 최소 저장 SMS 기준값.

### 수정됨
- 계산기 연산.

## [1.0.33] — 2018-02-11

### 추가됨
- 비정상 fragment 상태에 대한 예외 처리.

### 변경됨
- 리스트 레이아웃 재배치.

## [1.0.32] — 2018-01-29

### 추가됨
- 금액 표시 영역의 IME(가상 키보드) 입력 지원.

### 변경됨
- 최신 Android Studio 툴체인에서 컴파일 가능하도록 재정비.
- 거래 입력 시 날짜 설정.
- 월간 항목 날짜 설정.

### 수정됨
- 설정 화면의 null-pointer 가드.
- 입력 후 최신 항목이 갱신되지 않던 이슈.

## [1.0.31] — 2017-03-29

### 추가됨
- 리스트 화면 FAB.

### 변경됨
- 자산 현황 / 수익·지출 현황 페이지의 리스트뷰.
- 상태 fragment가 매번 데이터를 새로 가져오도록.

### 수정됨
- 빌드 경고 정리.

## [1.0.29] — 2017-01-21

### 수정됨
- 월간 항목 입력 경로.

### 변경됨
- 레거시 코드 제거.

## [1.0.28] — 2017-01-14

### 추가됨
- 리스트뷰에 섹션 제목 표시.
- 업데이트 알림 초기 타임스탬프.
- 거래 입력 form에 음수 입력 허용.

### 변경됨
- 리스트뷰 항목 재배치.

## [1.0.27] — 2017-01-13

### 수정됨
- Android 7.0(Nougat) 크래시.

### 추가됨
- 시작 시 런타임 권한 요청.

## [1.0.26] — 2017-01-12

이 changelog에 기록된 첫 버전.

### 수정됨
- ListView 중복 렌더링 이슈.
- 그래프 표시.

### 변경됨
- 그래프 관련 레이아웃 조정.

---

1.0.26 이전 히스토리는 초기 프로젝트 설정, Android Studio 마이그레이션,
패키지 ID 변경 단계로 정규 출시 주기에 들어가기 전이므로 별도 정리하지
않았습니다.
