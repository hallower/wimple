<?xml version="1.0" encoding="utf-8"?>
<!-- Background for the short-lived bubble that pops under a toolbar action when the
     matching badge first appears. Chip-shaped (12dp corners) with the app's primary
     color so it visually echoes the toolbar icon's interactive affordance, not a
     destructive/error one. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/md_theme_primary" />
    <corners android:radius="12dp" />
</shape>
