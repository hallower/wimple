<resources>

    <!-- Base application theme. Material 3 light-only — the parent is explicitly
         the .Light.* variant rather than .DayNight.* so the app stays on the
         light palette regardless of the system dark-mode setting. The explicit
         `<item>` overrides below wire the named palette colors onto the M3
         token attrs so they're picked up by every Material 3 component
         (toolbar, FAB, button, switch, …) consistently. -->
    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">
        <!-- M3 brand tokens -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_on_primary</item>
        <item name="colorPrimaryContainer">@color/md_theme_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_on_primary_container</item>
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_on_secondary</item>
        <item name="colorSecondaryContainer">@color/md_theme_secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/md_theme_on_secondary_container</item>
        <item name="colorTertiary">@color/md_theme_tertiary</item>
        <item name="colorOnTertiary">@color/md_theme_on_tertiary</item>
        <item name="colorTertiaryContainer">@color/md_theme_tertiary_container</item>
        <item name="colorOnTertiaryContainer">@color/md_theme_on_tertiary_container</item>

        <!-- M3 status / surface tokens -->
        <item name="colorError">@color/md_theme_error</item>
        <item name="colorOnError">@color/md_theme_on_error</item>
        <item name="colorErrorContainer">@color/md_theme_error_container</item>
        <item name="colorOnErrorContainer">@color/md_theme_on_error_container</item>
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorOnBackground">@color/md_theme_on_background</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_on_surface</item>
        <item name="colorSurfaceVariant">@color/md_theme_surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/md_theme_on_surface_variant</item>
        <item name="colorOutline">@color/md_theme_outline</item>

        <!-- AppCompat compatibility: a few legacy callers still read ?colorAccent
             (e.g. third-party libs); map it onto the M3 tertiary so it stays
             visually consistent with the rest of the palette. -->
        <item name="colorAccent">@color/md_theme_tertiary</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--
        Variant of AppTheme.NoActionBar that paints the system status bar with
        colorPrimaryDark so it visually extends the toolbar above. Used on
        activities whose layout root has fitsSystemWindows="true" + a colored
        Toolbar at the top — the values-v21 parent override forces statusBarColor
        to transparent (so SplashScreenActivity can render edge-to-edge), which
        on a Light surface would otherwise show the bare windowBackground and
        make OS icons hard to read. Overriding here keeps splash unaffected.
    -->
    <style name="AppTheme.NoActionBar.Tinted">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Toolbar foreground / popup overlays. Material 3 keeps the M2-era overlay
         names available (the material library still ships them) so existing
         layouts that reference @style/AppTheme.AppBarOverlay continue to
         resolve without a layout audit. -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="Transparent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorForeground">#fff</item>
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <style name="ButtonBarButton" />


</resources>
